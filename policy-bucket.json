{
    "Id": "PolicyForCloudFrontPrivateContent",
    "Statement": [
        {
            "Action": "s3:GetObject",
            "Condition": {
                "ArnLike": {
                    "AWS:SourceArn": "${lambda_arn}"
                }
            },
            "Effect": "Allow",
            "Principal": {
                "Service": "lambda.amazonaws.com",
                "AWS": "${lambda_iam_role_arn}"
            },
            "Resource": "arn:aws:s3:::${bucket}/*",
            "Sid": "AllowLambdaServicePrincipal"
        }
    ],
    "Version": "2012-10-17"
}
